load("@aspect_rules_ts//ts:defs.bzl", "ts_project")

ts_project(
    name = "transpile",
    srcs = ["index.ts"],
    allow_js = True,
    declaration = True,
    resolve_json_module = True,
    transpiler = "tsc",
    tsconfig = "//:ts_config",
)
