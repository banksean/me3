                       // This is a playground for testing generative code suggestion logging.
                       
                       function fib(n) {
                         if (n <= 1) {
                           return n;
                         }
                         return fib(n - 1) + fib(n - 2);
                       }
                       
                       console.log(fib(10));
                       
                       // Expected output:
                       
                       // This is hand-written:
                       console.log('hi');
                       
                       // This is generated by the AI:
                       console.log(fib(10));
                       
                       // hand-written
                       function test() {
                         return 0;
                       }
                       
                       // ai:
[codegemma, temp: 0.2] function test() {
[codegemma, temp: 0.2]   return 1;
[codegemma, temp: 0.2] }
[codegemma, temp: 0.2] 
[codegemma, temp: 0.2] console.log(test());
